services:
  - type: web
    name: contact-form-backend  # This can be different from repo name
    env: node
    plan: free
    rootDir: .  # Look in the root directory
    buildCommand: npm install
    startCommand: npm start
